# The PLE prelude (ple/) is now vendored and committed; see ple/README.md.
# `make bootstrap` refreshes it from the upstream RDE Bootstrap project.

# Generated combined Clafer model (recreate with: make)
needham-schroeder.cfr

# CV (proof) outputs (recreate with: make cv)
output/
